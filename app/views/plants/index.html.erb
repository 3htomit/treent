  <%= render 'components/navbar' %>
  <br>
  <br>
  <br>
  <br>
  <h1 class="mt-5">Plants</h1>
  <div class="main-container d-flex">
    <div class="cards d-flex flex-wrap">
      <% 21.times do |n| %>
        <%= render 'components/card', n: n %>
      <% end %>
    </div>
    <div class = "flex-column">
    <img id="map" src="https://en.mappy.com/front-services/map/44000-Nantes" alt="">
    <% if user_signed_in?%>
      <div class = "mt-3 btn btn-primary"><%= link_to "New Plant", new_my_plant_path(current_user) %></div>
    <%end%>
    </div>
  </div>