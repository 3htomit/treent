<%= link_to plant_path(plant.id) do %>
  <div class="suggestion-card" >
    <%= cl_image_tag plant.photos.first.key,
        height: 300, width: 300, crop: :fill,
        class: "suggestion-photo"%>
    <p class="suggestion-caption"><%= plant.name %></p>
  </div>
<% end %>
